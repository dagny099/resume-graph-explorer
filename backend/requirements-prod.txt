# Production dependencies — mlflow excluded (optional, ~200MB, exceeds Koyeb free RAM)

# Core Flask
flask>=3.0.0
flask-cors>=4.0.0
flask-socketio>=5.3.0
python-socketio>=5.10.0
werkzeug>=3.0.0

# WSGI server + async worker for Flask-SocketIO WebSockets.
# Must be declared here (not just pip-installed in the Dockerfile) so the
# gunicorn `--worker-class eventlet` command works regardless of build path.
# eventlet floor is the first release that imports cleanly on Python 3.12.
gunicorn>=21.2.0
eventlet>=0.35.2

# LLM clients
dspy-ai>=2.4.9
anthropic>=0.18.0
openai>=1.12.0
requests>=2.31.0

# RDF / graph
rdflib>=7.0.0
rdflib-jsonld>=0.6.2
networkx>=3.2

# Document processing
PyMuPDF>=1.23.0
pdfplumber>=0.10.0
python-docx>=1.1.0
python-magic>=0.4.27

# Validation / utilities
pydantic>=2.5.0
python-dateutil>=2.8.2
python-dotenv>=1.0.0
typing-extensions>=4.8.0
